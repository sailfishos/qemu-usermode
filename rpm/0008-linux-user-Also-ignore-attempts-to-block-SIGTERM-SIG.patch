From caaf943c41a562378500cc9997bbe7876d788688 Mon Sep 17 00:00:00 2001
From: Frajo Haider <f_haider@gmx.at>
Date: Thu, 23 Jan 2020 00:13:32 +0000
Subject: [PATCH 08/12] linux-user: Also ignore attempts to block SIGTERM,
 SIGINT and SIGHUP

---
 linux-user/signal.c | 3 +++
 1 file changed, 3 insertions(+)

diff --git a/linux-user/signal.c b/linux-user/signal.c
index 5ca6d62..36c7b25 100644
--- a/linux-user/signal.c
+++ b/linux-user/signal.c
@@ -225,6 +225,9 @@ int do_sigprocmask(int how, const sigset_t *set, sigset_t *oldset)
         /* Silently ignore attempts to change blocking status of KILL or STOP */
         sigdelset(&ts->signal_mask, SIGKILL);
         sigdelset(&ts->signal_mask, SIGSTOP);
+        sigdelset(&ts->signal_mask, SIGINT);
+        sigdelset(&ts->signal_mask, SIGTERM);
+        sigdelset(&ts->signal_mask, SIGHUP);
     }
     return 0;
 }
-- 
1.8.3-rc3

